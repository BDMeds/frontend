import { opacityVariant } from "@/lib/utils/variants";
import { motion } from "framer-motion";

const Notifications = () => {
  return <motion.div {...opacityVariant}>Notifications</motion.div>;
};

export default Notifications;
