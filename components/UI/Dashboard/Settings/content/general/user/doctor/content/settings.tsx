import { opacityVariant } from "@/lib/utils/variants";
import { motion } from "framer-motion";

const SettingsDoctor = () => {
  return <motion.div {...opacityVariant}>setting</motion.div>;
};

export default SettingsDoctor;
