import { opacityVariant } from "@/lib/utils/variants";
import { motion } from "framer-motion";

const Payments = () => {
  return <motion.div {...opacityVariant}>Payments</motion.div>;
};

export default Payments;
